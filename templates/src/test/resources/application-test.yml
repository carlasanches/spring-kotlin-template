spring:
  datasource:
    url: jdbc:h2:mem:db
    username: admin
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

